import React from 'react';

import * as S from './style';

function DocumentationLayout({ children }) {
  return <S.DocumentationLayout>{children}</S.DocumentationLayout>;
}

export default DocumentationLayout;
