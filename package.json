{
  "private": true,
  "name": "tour.js",
  "version": "0.0.1",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev-dashboard": "concurrently \"yarn workspace @tour.js/core build watch=true\" \"yarn workspace @tour.js/dashboard dev\"",
    "start-dashboard": "concurrently \"yarn workspace @tour.js/core build\" \"yarn workspace @tour.js/dashboard dev\""
  },
  "devDependencies": {
    "concurrently": "^7.0.0"
  }
}
